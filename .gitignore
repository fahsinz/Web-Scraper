# Python
.venv/
__pycache__/
*.py[cod]

# Secrets & credentials
.env
.cache
**/.cache

# Scraper caches (expensive API calls — keep locally, not in git)
cache/
geocode_cache.json
footprint_cache.json
data/geocode_cache.json
data/footprint_cache.json
data/osm_cache/

# SQLite working files (sidecars to awksion.db)
data/awksion.db-wal
data/awksion.db-shm

# Build artifacts
*.egg-info/
dist/
build/

# Dev/test scripts
test_spotify.py

# Claude Code
.claude/

# Selenium browser profile (large + per-machine)
chrome_profile/

# SQL_Database output folder (teammate SQL branch)
SQL_Database/
